---
import Layout from '@/layouts/Layout.astro';
import TemplateIcon from '@/components/TemplateIcon.astro';
import { templates, categories } from '@/data/templates';
import { siteUrl } from '@/lib/utils';

const categoryId = 'property';
const category = categories.find(c => c.id === categoryId)!;
const categoryTemplates = templates.filter(t => t.category === categoryId);

const title = 'Free Landlord & Property Templates for UK Landlords | Open Sheets';
const description = 'Free Excel templates for UK landlords: rental income tracking, MTD quarterly updates, property expenses, and Section 24 mortgage relief. HMRC SA105-aligned.';

const softwareAppSchemas = categoryTemplates.map((t) => ({
  '@type': 'SoftwareApplication',
  name: t.name,
  applicationCategory: 'BusinessApplication',
  operatingSystem: 'Excel, Google Sheets, LibreOffice',
  offers: { '@type': 'Offer', price: '0', priceCurrency: 'GBP' },
  url: `${siteUrl}/templates/${t.id}`,
  description: t.description,
}));

const otherCategories = categories.filter(c => c.id !== 'all' && c.id !== categoryId);
---

<Layout title={title} description={description} jsonLd={softwareAppSchemas}>
  <div class="bg-slate-50 border-b border-slate-200">
    <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
      <nav class="flex text-sm text-slate-500">
        <a href="/" class="hover:text-sky-700 transition-colors">Home</a>
        <span class="mx-2">/</span>
        <span class="text-slate-900 font-medium">Landlord & Property</span>
      </nav>
    </div>
  </div>

  <section class="bg-slate-50 border-b border-slate-200">
    <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-12 lg:py-16">
      <span class="inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-medium bg-amber-100 text-amber-700 mb-4">Free Templates</span>
      <h1 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-slate-900 mb-4 leading-tight">
        Landlord & Property <span class="text-sky-700">Templates</span>
      </h1>
      <p class="text-lg text-slate-600 max-w-2xl mb-6">
        Rental income tracking, property expense logs, and MTD quarterly updates — all built for UK landlords. Handle Section 24 mortgage relief, SA105 tax returns, and Making Tax Digital with confidence.
      </p>
      <div class="flex flex-wrap gap-3 text-sm text-slate-500">
        <span class="inline-flex items-center gap-1">
          <svg class="w-4 h-4 text-emerald-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
          {categoryTemplates.length} free templates
        </span>
        <span class="inline-flex items-center gap-1">
          <svg class="w-4 h-4 text-emerald-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
          SA105 tax-aligned
        </span>
        <span class="inline-flex items-center gap-1">
          <svg class="w-4 h-4 text-emerald-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
          Section 24 ready
        </span>
      </div>
    </div>
  </section>

  <section class="py-16 bg-white">
    <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid sm:grid-cols-2 gap-6">
        {categoryTemplates.map((template) => (
          <a
            href={`/templates/${template.id}`}
            class="group bg-slate-50 rounded-xl p-6 border border-slate-200 hover:border-sky-300 hover:shadow-md transition-all"
          >
            <div class={`w-12 h-12 rounded-xl ${template.color} flex items-center justify-center text-white mb-4`}>
              <TemplateIcon icon={template.icon} class="w-6 h-6" />
            </div>
            <h3 class="font-semibold text-slate-900 mb-2 group-hover:text-sky-700 transition-colors">{template.name}</h3>
            <p class="text-sm text-slate-600 mb-4">{template.description}</p>
            <ul class="space-y-1.5">
              {template.features.slice(0, 3).map((feature) => (
                <li class="flex items-start gap-2 text-xs text-slate-500">
                  <svg class="w-3.5 h-3.5 text-sky-700 flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                  <span>{feature}</span>
                </li>
              ))}
            </ul>
            <div class="mt-4 pt-4 border-t border-slate-200 flex items-center text-sm font-medium text-sky-700">
              View template
              <svg class="w-4 h-4 ml-1 group-hover:translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3"/></svg>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <section class="py-16 bg-slate-50 border-y border-slate-200">
    <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid lg:grid-cols-2 gap-12 items-center">
        <div>
          <h2 class="text-2xl sm:text-3xl font-bold text-slate-900 mb-4">Built for UK Landlords</h2>
          <p class="text-slate-600 mb-6">
            UK landlord tax is complex — Section 24 mortgage restrictions, SA105 reporting, and now Making Tax Digital quarterly updates. Our templates handle the calculations so you stay compliant without the headache.
          </p>
          <ul class="space-y-3">
            <li class="flex items-start gap-3">
              <svg class="w-5 h-5 text-sky-700 flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
              <span class="text-slate-600">Section 24 mortgage interest relief (20% tax credit)</span>
            </li>
            <li class="flex items-start gap-3">
              <svg class="w-5 h-5 text-sky-700 flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
              <span class="text-slate-600">SA105 box mapping for annual tax returns</span>
            </li>
            <li class="flex items-start gap-3">
              <svg class="w-5 h-5 text-sky-700 flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
              <span class="text-slate-600">MTD quarterly structure ready for 2026</span>
            </li>
          </ul>
        </div>
        <div class="bg-white rounded-xl p-6 border border-slate-200">
          <h3 class="font-semibold text-slate-900 mb-4">Landlord Tax Changes You Need to Know</h3>
          <ul class="space-y-3 text-sm text-slate-600">
            <li class="flex items-start gap-2"><span class="text-amber-500 font-bold">⚠️</span> <strong>Section 24:</strong> Mortgage interest is no longer deductible. You get a 20% tax credit instead.</li>
            <li class="flex items-start gap-2"><span class="text-amber-500 font-bold">⚠️</span> <strong>MTD 2026:</strong> Landlords with £50k+ income must submit quarterly from April 2026.</li>
            <li class="flex items-start gap-2"><span class="text-amber-500 font-bold">⚠️</span> <strong>3% Stamp Duty:</strong> Additional properties attract 3% extra SDLT.</li>
            <li class="flex items-start gap-2"><span class="text-amber-500 font-bold">⚠️</span> <strong>Wear & Tear:</strong> Replacement Relief replaced the old 10% allowance.</li>
          </ul>
          <p class="text-xs text-slate-400 mt-4">Our templates stay current with HMRC rules. Check back for updates.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="py-16 bg-white">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-10">
        <span class="inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-medium bg-sky-100 text-sky-700 mb-4">FAQ</span>
        <h2 class="text-2xl font-bold text-slate-900 mb-2">Landlord Template Questions</h2>
      </div>
      <div class="space-y-4">
        <details class="group bg-slate-50 rounded-lg border border-slate-200">
          <summary class="flex justify-between items-center cursor-pointer p-4 font-medium text-slate-900">
            Do these templates work for Airbnb and short-term lets?
            <svg class="w-5 h-5 text-slate-400 group-open:rotate-180 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
          </summary>
          <div class="px-4 pb-4 text-sm text-slate-600">
            Yes. Record rental income and expenses the same way. Note that furnished holiday lettings have different tax rules — check with an accountant if this applies to you.
          </div>
        </details>
        <details class="group bg-slate-50 rounded-lg border border-slate-200">
          <summary class="flex justify-between items-center cursor-pointer p-4 font-medium text-slate-900">
            When does MTD for landlords start?
            <svg class="w-5 h-5 text-slate-400 group-open:rotate-180 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
          </summary>
          <div class="px-4 pb-4 text-sm text-slate-600">
            Landlords with gross property income over £50,000 must start quarterly reporting from April 2026. Those with income over £30,000 follow from April 2027. Our <a href="/templates/mtd-quarterly-property" class="text-sky-700 hover:underline">MTD Quarterly template</a> is ready now.
          </div>
        </details>
        <details class="group bg-slate-50 rounded-lg border border-slate-200">
          <summary class="flex justify-between items-center cursor-pointer p-4 font-medium text-slate-900">
            What expenses can I claim against rental income?
            <svg class="w-5 h-5 text-slate-400 group-open:rotate-180 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
          </summary>
          <div class="px-4 pb-4 text-sm text-slate-600">
            Allowable expenses include letting agent fees, buildings insurance, maintenance and repairs, council tax (if paid by you), utilities (if paid by you), and ground rent. Capital improvements (new kitchen, extension) are not deductible — they add to your property\'s base cost for Capital Gains Tax instead.
          </div>
        </details>
      </div>
    </div>
  </section>

  <section class="py-16 bg-slate-50 border-y border-slate-200">
    <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-10">
        <h2 class="text-2xl font-bold text-slate-900 mb-2">Explore Other Categories</h2>
        <p class="text-slate-600">More free templates for your business</p>
      </div>
      <div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-4">
        {otherCategories.map((cat) => (
          <a
            href={`/${cat.id === 'core' ? 'core-bookkeeping' : cat.id === 'tax' ? 'tax-vat' : cat.id === 'reports' ? 'financial-reports' : cat.id === 'business' ? 'business-tools' : cat.id}`}
            class="group bg-white rounded-xl p-5 border border-slate-200 hover:border-sky-300 hover:shadow-md transition-all text-center"
          >
            <h3 class="font-semibold text-slate-900 group-hover:text-sky-700 transition-colors">{cat.label}</h3>
            <p class="text-xs text-slate-500 mt-1">{templates.filter(t => t.category === cat.id).length} templates</p>
          </a>
        ))}
      </div>
    </div>
  </section>

  <section class="py-16 bg-sky-700">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h2 class="text-3xl font-bold text-white mb-4">Let Your Properties Work for You</h2>
      <p class="text-lg text-sky-100 mb-8">Track income, manage expenses, and stay HMRC-compliant — all with free templates designed for UK landlords.</p>
      <a href="/templates" class="inline-flex items-center justify-center bg-white text-sky-700 hover:bg-sky-50 h-14 px-8 text-base font-semibold rounded-lg transition-colors">
        Browse All Templates
      </a>
      <p class="text-sm text-sky-200 mt-6">Free downloads, no email required. Works in Excel and Google Sheets.</p>
    </div>
  </section>
</Layout>
